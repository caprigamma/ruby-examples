print "Cuantos numeros quiere imprimir: "
n= gets.chomp.to_i
#(1..n).each { |var_block | print "#{var_block".}

#bloque usando each imprimir numeros con puntos
(1..n).each  do |var_block|
    print "#{var_block}."

end