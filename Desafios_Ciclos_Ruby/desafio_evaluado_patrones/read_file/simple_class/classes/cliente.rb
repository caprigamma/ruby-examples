class Cliente
    def full_name(name,last_name)
    def get_address(dirección,comuna)
    
    puts "El nombre completo es: #{name.capitalize}, #{last_name.capitalize}"
    puts "La dirección es: #{dirección.capitalize}, #{comuna.capitalize}"
    end
end